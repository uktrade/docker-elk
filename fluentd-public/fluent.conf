<source>
  @type syslog
  protocol_type "#{ENV['FLUENTD_PROTOCOL']}"
  bind "#{ENV['FLUENTD_BIND_IP']}"
  port "#{ENV['FLUENTD_PORT']}"
  tag "#{ENV['FLUENTD_TAG']}"
</source>

<match **>
  @type copy
  <store>
    @type stdout
  </store>
  <store>
    @type kinesis_streams
    region "#{ENV['AWS_REGION']}"
    stream_name "#{ENV['KINESIS_STREAM_NAME']}"
  </store>
</match>
