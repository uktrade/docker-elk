output.elasticsearch:
  hosts: ["${ES_HOST}"]
  protocol: "https"
  username: "${ES_USERNAME}"
  password: "${ES_PASSWORD}"
  ssl.verification_mode: none

index: "dit-gsuite"

filebeat.modules:
  - module: gsuite
    login:
      enabled: true
      var.jwt_file: "/var/tmp/credentials_file.json"
      var.delegated_account: "user@example.com"

monitoring:
  enabled: true
  elasticsearch:
    username: "${ES_MONITORING_USERNAME}"
    password: "${ES_MONITORING_PASSWORD}"
