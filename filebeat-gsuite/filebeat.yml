output.console:
  pretty: true

filebeat.modules:
  - module: gsuite
    user_accounts:
      enabled: true 
      var.jwt_file: "/usr/share/filebeat/credentials_file.json"
      var.delegated_account: lyndon.garvey@data.trade.gov.uk
    saml:
      enabled: true
      var.jwt_file: "/usr/share/filebeat/credentials_file.json"
      var.delegated_account: lyndon.garvey@data.trade.gov.uk
    login:
      enabled: true 
      var.jwt_file: "/usr/share/filebeat/credentials_file.json"
      var.delegated_account: lyndon.garvey@data.trade.gov.uk
    admin:
      enabled: true 
      var.jwt_file: "/usr/share/filebeat/credentials_file.json"
      var.delegated_account: lyndon.garvey@data.trade.gov.uk
    drive:
      enabled: true
      var.jwt_file: "/usr/share/filebeat/credentials_file.json"
      var.delegated_account: lyndon.garvey@data.trade.gov.uk
    groups:
      enabled: true 
      var.jwt_file: "/usr/share/filebeat/credentials_file.json"
      var.delegated_account: lyndon.garvey@data.trade.gov.uk

monitoring:
  enabled: false

logging.level: debug

